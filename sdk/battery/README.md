# Battery and Power SDK

Battery and power policy module.

The first SDK release does not expose the small power/system button as a user-customizable key. It remains reserved for:

- power on/off
- Bluetooth reset
- bootloader entry
- future maintenance flows

Battery state is currently exposed mainly through official state query and desktop sync behavior.
