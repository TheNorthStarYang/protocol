# SDK Core

Core SDK package:

```text
include/   public SDK headers
lib/       binary core library and startup object
linker/    linker script
```

The binary core keeps BLE, USB, Flash, power management, official protocol behavior, and SDK bridge internals outside the public source tree.
