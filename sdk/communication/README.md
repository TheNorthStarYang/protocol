# Communication SDK

Communication and custom command module.

Public APIs are declared in:

```text
../ahakey_core/include/ahakey_protocol.h
```

User custom commands should use `0xA0 - 0xEF`.

Official AhaKey commands use `0x00 - 0x9F`. System/debug/factory use `0xF0 - 0xFF`.
